# Local AI Models

Local language models run inference directly on a user's machine instead of requiring every request to be sent to a remote model API.

## Benefits

Local inference can provide greater data privacy, offline capability, and control over model execution.

Ollama simplifies running local models such as Qwen on a developer workstation.