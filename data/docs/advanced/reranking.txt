Reranking is a second-stage retrieval process. An initial retriever first returns a candidate set of potentially relevant documents. A reranker then scores those candidates more carefully using the relationship between the full query and each candidate. Because reranking is applied to a smaller candidate set, a more computationally expensive model can be used to improve the final ordering.